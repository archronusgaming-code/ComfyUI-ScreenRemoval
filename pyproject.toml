[project]
name = "ComfyUI-ScreenRemoval"
version = "0.1.0"
description = "Automatic patio and window screen removal for ComfyUI"
authors = [
    { name="Archronus" }
]
license = { text = "MIT" }
requires-python = ">=3.10"

dependencies = [
    "numpy>=2.0",
    "opencv-python>=4.10",
    "scipy>=1.13",
    "Pillow>=11.0"
]